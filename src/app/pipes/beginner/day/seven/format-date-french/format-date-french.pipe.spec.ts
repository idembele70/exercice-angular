import { FormatDateFrenchPipe } from './format-date-french.pipe';

describe('FormatDateFrenchPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatDateFrenchPipe();
    expect(pipe).toBeTruthy();
  });
});
